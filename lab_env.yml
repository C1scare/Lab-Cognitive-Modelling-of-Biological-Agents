name: lab_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - numpy
  - pydantic
  - plotly
  - matplotlib
  - pip
  - pip:
      - dash-bootstrap-components
      - dash
      - retry
      - optuna